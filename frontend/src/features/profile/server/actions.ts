"use server";

// This file is for your Next.js Server Actions.
// Server Actions are functions that you can write once and use on both the server and the client.
// They are ideal for data mutations (e.g., creating, updating, or deleting data).

// Example:
// export async function createUser(formData: FormData) {
//   const name = formData.get('name');
//   // ... logic to create a user in your database
// }
